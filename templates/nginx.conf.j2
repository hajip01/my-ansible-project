server {
    listen {{ nginx_port }} default_server;
    listen [::]:{{ nginx_port }} default_server;
    
    root {{ nginx_root }};
    index index.html index.htm;
    
    server_name _;
    
    location / {
        try_files $uri $uri/ =404;
    }
}
